# 方案设计文档：Hy3 代码过程评估与错误定位系统

> 项目：`hy3-code-process-localizer`
> 任务背景：腾讯犀牛鸟开源实战任务 ——「可验证场景：过程评估与错误定位」（Task 2）
> 模型：Tencent Hunyuan Hy3（通过腾讯云 TokenHub 的 OpenAI 兼容接口调用）
> 文档维护：随代码同步更新

---

## 1. 文档目的与范围

本文档说明系统的**方案设计**，覆盖：任务定义、总体架构、各核心模块的设计与职责、三层错误定位模型、错误类型体系、验证方案、实验设计与结果、已知局限。代码实现所对应的接口与字段以 `src/hcp_eval/` 下源码为准；数据集来源见 `docs/dataset_notes.md`，方法细节见 `docs/method.md`。

---

## 2. 背景与问题定义

### 2.1 任务来源

本项目对应犀牛鸟开源实战任务中的第二个任务：**「可验证场景：过程评估与错误定位」**。传统代码评测（如 HumanEval / MBPP 的 pass@k）只判断「最终答案是否正确」，而忽略了解题**过程**是否成立。

### 2.2 核心问题：伪成功（Unsupported Success）

大模型可能输出一个**最终测试通过、但推理过程存在缺陷**的解，例如：

- 复杂度声明与实现不符（声称 O(n)，实为 O(n²)）；
- 计划（S3 选定算法）与最终代码（S7）不一致；
- 靠巧合或边界特例通过测试，但证明/逻辑链存在断层；
- 隐含了未经验证的假设。

这类「**正确但过程不成立**」的样本（伪成功）在只看最终答案的评测中被误判为完美。本系统的目标就是**在答案正确之外，额外评估其过程是否站得住脚，并精确定位缺陷所在**。

### 2.3 设计目标

1. 对每道题输出 **S1–S7 七步结构化推理过程 + 可执行代码**；
2. 给出**多层错误定位**：过程步骤 → 规格条款 → 代码行；
3. 识别并标记**伪成功**样本；
4. 融合**执行测试 + 静态分析 + 覆盖率（SBFL）** 多源证据，降低单一信号误判。

---

## 3. 设计原则

| 原则 | 说明 |
|------|------|
| **执行证据优先** | 测试失败是不可否认的事实，LLM 的语义判断不能覆盖确定性失败 |
| **静态规则其次** | AST / 规则检查提供快速、确定性的信号 |
| **Hy3 Reviewer 补充语义** | 仅在执行与静态证据不足时调用模型做高层语义判断 |
| **冲突以测试为准** | 当不同来源证据冲突时，按来源优先级裁决（见 §7.4） |
| **不训练、不微调** | 仅通过 Hy3 API 调用模型能力，模型权重不变 |

---

## 4. 总体架构

```
EvalPlus MBPP+ / HumanEval+  (官方开源基准，运行时经 API 拉取)
        │
        ▼
┌─────────────────────┐
│  Dataset Adapter    │ ──→ ProblemSpec（统一数据格式，含规格条款 C1..Cn）
└─────────┬───────────┘
          │
          ▼
┌─────────────────────┐
│   Hy3 Solver        │ ──→ S1–S7 过程步骤 + Python 代码（SolutionRecord）
└─────────┬───────────┘
          │
          ▼
┌─────────────────────┐
│    Executor         │ ──→ base / plus 测试执行结果（Sandbox）
│  (Sandbox Runner)   │
└─────────┬───────────┘
          │
   ┌──────┼───────────────┬──────────────┐
   ▼      ▼               ▼              ▼
┌────────┐ ┌────────┐ ┌──────────┐ ┌──────────────┐
│ AST    │ │ Rule   │ │ Coverage │ │ Hy3 Reviewer │
│Checker │ │Checker │ │ + SBFL   │ │ (语义补充)    │
│(静态)  │ │(过程)  │ │(Ochiai)  │ │              │
└───┬────┘ └───┬────┘ └────┬─────┘ └──────┬───────┘
    │          │           │              │
    ▼          ▼           ▼              ▼
┌──────────────────────────────────────────────┐
│              Evidence Merger                  │
│  合并 / 去重 / 优先级排序 / 冲突裁决           │
└──────────────────────┬───────────────────────┘
                       ▼
┌──────────────────────────────────────────────┐
│              Error Localizer                  │
│  first_error_step / violated_clause /         │
│  primary_error_type / unsupported_success /   │
│  suspect_code_lines  →  EvaluationResult      │
└──────────────────────┬───────────────────────┘
                       ▼
┌──────────────────────────────────────────────┐
│          Metrics & Report Writer              │
│  CSV / JSONL / Markdown（含综合报告）         │
└──────────────────────────────────────────────┘
```

---

## 5. 核心模块设计

### 5.1 数据接入层（Dataset Adapter）

- 通过 **EvalPlus 官方 API**（`evalplus.data.get_mbpp_plus()` / `get_human_eval_plus()`）加载真实基准，运行时拉取、可离线缓存为 JSONL。
- 统一抽象为 `ProblemSpec`：
  - `task_id`、`dataset`、`prompt`、`entry_point`；
  - `canonical_solution`（参考答案，用于 plus 测试对比）；
  - `base_tests` / `plus_tests`（原始 / 增强断言）；
  - `clauses`：从题干抽取的**规格条款** `C1, C2, …`，作为错误定位的第二层锚点。
- **真实性说明**：数据集为 EvalPlus 官方公开基准，**非自行编造**；大体积原始文件不入库（见 `docs/dataset_notes.md` 与 `.gitignore`）。

### 5.2 过程生成层（Hy3 Solver）

- 调用 Hy3（TokenHub OpenAI 兼容接口，模型名 `hy3`，`reasoning_effort=high`），要求模型**只返回 JSON**。
- 结构化输出约定（`prompt_builder.SOLVER_SYSTEM_PROMPT`）：
  - 顶层字段：`steps`（7 步）、`complexity`（含 `time`/`space`）、`code`（完整可运行函数）；
  - 每个步骤含 `id`(S1–S7)、`type`、`claim`（推理内容）、`related_clauses`（关联的条款 ID）。
- 客户端 `Hy3Client` 具备**重试 / 指数退避 / 超时**配置（`hy3_max_retries`、`hy3_request_timeout`），以抵御偶发限流（HTTP 429/402）。

### 5.3 证据采集层

#### 5.3.1 执行器（Sandbox Runner）
- 在受限子进程中运行候选代码，对 `base_tests`、`plus_tests` 逐条执行，记录通过/失败与错误信息（超时、异常、断言失败分别归类）。
- `prepare_function_code()` 处理入口函数名对齐，确保 HumanEval+ 的 `check()` 整体（含 setup 赋值）被正确执行。

#### 5.3.2 AST 静态检查（ASTChecker）
- 解析代码 AST，检测语法错误、缺失函数、危险模式（`eval`/`exec`）、基础结构问题。
- 产物为 `static_check` 来源的证据（优先级 60）。

#### 5.3.3 规则检查（RuleChecker）
- 针对过程做确定性规则校验，包含：
  - **复杂度一致性** `check_complexity_consistency`：用 **AST 嵌套循环深度**判断代码真实复杂度，仅当存在真实嵌套且声明亚二次（如声称 O(n) 但实为 O(n²)）才报 `COMPLEXITY_MISCLAIM`；
  - **计划—代码一致性** `check_plan_code_alignment`：比对 S3 选定算法与 S7 实现，采用**否定词感知**匹配（如 "without manual iteration" 不会误判为要求循环）+ AST 确认代码中存在对应构造，否则报 `PLAN_CODE_MISMATCH`。
- **已修复的两处误报**（验证集 §12.3 发现并修正）：
  1. 原以「缩进≥8」判定复杂度误报——单层 `for` 内调用 `append` 也会被冤枉；
  2. 原将 S3 中 "formula **for**" / "**for** exact" 等字面 "for" 误当「使用循环算法」。

#### 5.3.4 基于覆盖率的错误定位（SBFL / Ochiai）
- 借助 `coverage.py` 逐测试收集行覆盖；
- 采用 **Ochiai** 公式对每行计算可疑度：

  ```
  score(line) = failed_cover(line)
                ─────────────────────────────────────────────
                sqrt( total_failed × (failed_cover(line) + passed_cover(line)) )
  ```

- 输出 `SuspectCodeLine` 列表（按分数降序），作为第三层（代码行）定位。
- 仅当存在失败测试时计算；全部通过则返回空（无嫌疑行）。

### 5.4 证据融合层（EvidenceMerger）

多源证据统一为 `EvidenceRecord`（`source` / `status` / `target_step` / `target_clause` / `error_type` / `confidence`），融合策略：

- **来源优先级**（数值越高越权威）：

  | 来源 | 优先级 |
  |------|--------|
  | plus_test | 100 |
  | base_test | 90 |
  | sbfl | 80 |
  | static_check | 60 |
  | hy3_review | 50 |

- **去重**：以 `(target_step, error_type, source)` 去重，保留最高优先级版本。
- **首错步** `determine_first_error_step`：取所有 `contradicted` 证据中**最早**的步骤（S1→S7）。
- **主错误类型** `determine_primary_error_type`：按步骤顺序 + 置信度取首个 `contradicted` 类型。
- **违规条款** `find_violated_clause`：统计被违反最多的条款。
- **伪成功检测** `detect_unsupported_success`：当且仅当 `final_correct=True` 且（S1–S5 存在 `contradicted` 证据，或存在 `UNJUSTIFIED_ASSUMPTION`/`REASONING_GAP`/`ALGORITHM_INVALID`/`SPEC_MISREAD` 类过程缺陷）。**仅 `contradicted` 状态计入**，低置信度 `warning` 不会误触发。

### 5.5 错误定位层（ErrorLocalizer）

`ErrorLocalizer.evaluate()` 编排完整七阶段流水线（见 `error_localizer.py`）：

1. 执行 base 测试；2. 执行 plus 测试（含 oracle 对比）；3. 收集测试证据；4. AST 静态分析；5. 规则过程检查；6. SBFL（可选，较慢）；7. 证据融合与最终判定。

输出 `EvaluationResult`，含 `final_correct` / `process_correct` / `first_error_step` / `violated_clause` / `primary_error_type` / `unsupported_success` / `suspect_code_lines` / `evidence`。

### 5.6 指标与报告

- `MetricsCalculator` 聚合：`Final Acc` / `Base Pass` / `Plus Pass` / `Process Acc` / 错误类型分布 / 难度分层；验证指标（定位准确率、误报率、类型准确率）在有金标准时计算。
- `ReportWriter` 产出 CSV / JSONL / Markdown 分析报告；`make_combined_report.py` 汇总多数据集生成 `combined_report.md`。

---

## 6. 三层错误定位模型

| 层级 | 锚点 | 来源 | 输出字段 |
|------|------|------|----------|
| 第一层 过程步骤 | S1–S7 | 静态检查 / 规则 / 测试映射 | `first_error_step` |
| 第二层 规格条款 | C1, C2, … | 证据关联条款 | `violated_clause` |
| 第三层 代码行 | 行号 | SBFL（Ochiai） | `suspect_code_lines` |

该分层实现了「从**为什么错**（步骤/条款）到**错在哪一行**（代码）」的可追溯链路，是本任务「过程评估与错误定位」的核心价值。

---

## 7. 错误类型体系（14 类）

| 错误类型 | 含义 |
|----------|------|
| `FORMAT_ERROR` | 输出格式不符合要求 |
| `SPEC_MISREAD` | 误解题目要求 |
| `CONDITION_MISSING` | 未处理边界 / 条件 |
| `UNJUSTIFIED_ASSUMPTION` | 无依据假设 |
| `ALGORITHM_INVALID` | 算法选择根本错误 |
| `REASONING_GAP` | 推理链逻辑断层 |
| `COMPLEXITY_MISCLAIM` | 复杂度声明与实现矛盾 |
| `EDGE_CASE_FAILURE` | 边界 / 极端输入失败 |
| `PLAN_CODE_MISMATCH` | 实现与计划不一致 |
| `PROCESS_RESULT_MISMATCH` | 结果对但过程错（伪成功） |
| `RUNTIME_ERROR` | 执行抛异常 |
| `TIMEOUT` | 超时 |
| `SECURITY_VIOLATION` | 代码安全隐患 |
| `UNKNOWN` | 未分类 |

---

## 8. 验证方案

### 8.1 §12.3 过程定位验证集（真实样本，agent 标注金标准）
- 从 60 个真实 Hy3 输出中**分层抽样 13 个**，覆盖干净解 / 真实失败解 / 伪成功候选。
- 由 agent 依据 S1–S7 rubric 独立标注金标准（`gold_annotations.json`），已声明为**非独立人工核验**。
- 复验结果（修复 rule_checker 误报后）：**错误检测 100%、首错步 100%、错误类型 100%、误报率 0%**。
- 该验证集在过程中**成功揪出并推动修复**了静态检查器两处误报 bug。

### 8.2 §12.4 对抗验证集（受控样本）
- 构造 8 个**已知金标准**的受控样本（`data/adversarial_set.json`）：复杂度误报、缺失步骤、计划—代码矛盾、运行时错误、断言失败、负样本 ×3。
- 结果：**错误检测 87.5%、步级 80%、类型 80%、误报率 0%、伪成功检测 100%**。

### 8.3 诚实声明
- EvalPlus **无过程级金标准**，自动实验的定位/类型准确率无法用外部 ground-truth 计算；本报告过程准确率依据本系统自有 S1–S7 rubric 判定。
- §12.3 金标准为 agent 标注，验证集偏向「正确解」，更硬的对抗测试见 §12.4。

---

## 9. 实验设计与结果

### 9.1 设置
- 模型：Hy3（`hy3`，`reasoning_effort=high`）；推理在腾讯云 TokenHub 完成，评测 / 定位均在本机执行。
- 基准：MBPP+（主）、HumanEval+（对照）；TokenHub 免费额度内真实运行。

### 9.2 总体结果（130 题合并）

| 指标 | 数值 |
|------|------|
| 总题数 | 130 |
| **最终准确率** | **88.46%** |
| Base 通过率 | 98.46% |
| Plus 通过率 | 89.23% |
| **过程准确率** | **56.15%** |
| **伪成功** | **19 (14.62%)** |

### 9.3 分数据集

| 指标 | MBPP+ (100) | HumanEval+ (30) |
|------|-------------|-----------------|
| Final Acc | 85.00% | 100.00% |
| Process Acc | 62.00% | 36.67% |
| 伪成功数 | 2 (2.00%) | 17 (56.67%) |

> **说明（检查器版本一致性）**：MBPP+ 100 题为**修复后** `rule_checker` 真实重跑；HumanEval+ 30 题因 TokenHub 免费额度在修复后复跑时耗尽（HTTP 402），保留**修复前**检查器产出的真实结果，其过程指标含已知误报、被低估。额度恢复后应用修复后检查器复跑 HumanEval+（见 §10）。

### 9.4 过程错误归因（57 道过程判错题）

`PLAN_CODE_MISMATCH` 40.4% > `COMPLEXITY_MISCLAIM` 28.1% > `EDGE_CASE_FAILURE` 19.3% > `REASONING_GAP` 7.0% > `RUNTIME_ERROR` 5.3%。

**结论**：Hy3 在 EvalPlus 上代码正确性良好，但**约 44% 的题目过程存在缺陷**，且伪成功 14.62%——这正是过程评估要揭示的核心价值。

---

## 10. 已知局限与未来工作

| 项 | 说明 |
|----|------|
| TokenHub 额度 | HumanEval+ 待修复后检查器复跑；恢复额度后需补跑刷新综合报告 |
| 算法替换漏报 | 已知局限：声称某算法（如 hash）但用循环实现时，计划—代码匹配可能漏报（ADV-03 类） |
| 金标准来源 | §12.3 为 agent 标注，建议引入独立人工核验以提升可信度 |
| 样本量 | MBPP+ 100 / HumanEval+ 30，结论为初步趋势，可扩至全量 378 / 164 |
| 规则覆盖面 | 静态 / 规则检查为启发式，长尾错误类型仍依赖测试失败或 Reviewer |

---

## 11. 复现指南

```bash
# 1. 克隆与安装
git clone https://github.com/cqlvsy/hy3-code-process-localizer.git
cd hy3-code-process-localizer
python -m venv .venv && source .venv/bin/activate
pip install -e ".[dev]"

# 2. 配置 Hy3（TokenHub，OpenAI 兼容）
cp .env.example .env
# .env 中设置：
#   HY3_PROVIDER=openai
#   HY3_BASE_URL=https://tokenhub.tencentmaas.com/v1
#   HY3_API_KEY=<你的 TokenHub Key>      # 仅本地，不入库
#   HY3_MODEL=hy3
#   HY3_REASONING_EFFORT=high

# 3. 运行实验
hcp-eval run --dataset mbppplus --sample-size 100     # 主实验
hcp-eval run --dataset humanevalplus --sample-size 30 # 对照

# 4. 稳健可续跑脚本（逐题落盘，适合大批量）
python scripts/run_mbpp100.py
python scripts/run_humaneval30.py

# 5. 生成综合报告
python scripts/make_combined_report.py
```

> 密钥仅存于本地 `.env`（已被 `.gitignore` 排除），**绝不**进入仓库。

---

## 12. 安全与合规

- **密钥隔离**：API Key 仅本地 `.env`，全仓库历史扫描确认无任何真实凭据入库。
- **数据合规**：仅使用 EvalPlus 公开发布基准（Apache-2.0 / MIT），无个人隐私数据。
- **执行隔离**：候选代码在受限子进程 + 超时保护下运行，无网络 / 文件系统越权。
- **不训练模型**：仅推理调用，权重与训练数据不受影响。

---

## 13. 文档索引

| 文档 | 内容 |
|------|------|
| `README.md` | 项目总说明、架构图、使用方式 |
| `docs/method.md` | 方法学细节、流水线各阶段 |
| `docs/dataset_notes.md` | 数据集来源与链接（EvalPlus 官方） |
| `docs/error_taxonomy.md` | 错误类型体系详述 |
| `results/combined_report.md` | 综合实验报告 |
| `results/validation/validation_report.md` | §12.3 验证报告 |
| `results/validation/adversarial_report.md` | §12.4 对抗验证报告 |

---

*本文档由项目维护者随代码同步更新。模型推理在腾讯云 TokenHub 完成，其余评测 / 定位均在本机执行。*
